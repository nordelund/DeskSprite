# 上游同步指南

本项目 fork 自 [rainnoon/oc-claw](https://github.com/rainnoon/oc-claw)，在其基础上进行了定制开发。
本文档说明如何安全地将上游更新合并进来。

---

## Remote 配置

```
origin    → https://github.com/nordelund/DeskSprite.git  （你的 fork）
upstream  → https://github.com/rainnoon/oc-claw.git      （原始上游）
```

---

## 日常同步流程

### 第一步：拉取上游最新代码

```bash
git fetch upstream
```

### 第二步：查看上游有哪些新提交

```bash
git log --oneline upstream/main ^HEAD
```

如果输出为空，说明已是最新，无需操作。

### 第三步：合并上游

```bash
git merge upstream/main
```

---

## 处理冲突

合并后如有冲突，Git 会提示哪些文件需要手动解决。

### 查看冲突文件

```bash
git status
```

### 查看冲突内容

```bash
git diff --diff-filter=U
```

### 解决冲突

用编辑器打开冲突文件，冲突区域格式如下：

```
<<<<<<< HEAD
你的改动
=======
上游的改动
>>>>>>> upstream/main
```

保留需要的内容，删除标记行，然后：

```bash
git add <已解决的文件>
git merge --continue
```

### 放弃本次合并（如遇复杂冲突想重来）

```bash
git merge --abort
```

---

## 哪些文件容易冲突

本项目对以下上游文件有定制改动，合并时需重点关注：

| 文件 | 定制内容 |
|------|---------|
| `frontend/src/lib/codexPet.ts` | 状态名重命名、ANIMATION_ROWS 行映射 |
| `frontend/src/components/MiniPetMascot.tsx` | prop 类型改为 `sourceState`，接入状态机 hook |
| `frontend/src/components/SpritePet.tsx` | `done` 改为循环状态 |
| `frontend/src/Mini.tsx` | MiniPetMascot 传参方式变更 |
| `frontend/src/DemoMascot.tsx` | 同上 |

**不会冲突的文件**（本项目新增，上游没有）：

- `frontend/src/lib/usePetAnimPhase.ts`
- `frontend/public/assets/builtin/fox-pink/`

---

## 定位你的定制改动

本项目定制代码以注释标记，合并冲突时可快速识别：

```typescript
// ▼ DeskSprite customization
...
// ▲ DeskSprite customization end
```

---

## 同步后推送到自己的 fork

```bash
git push origin main
```

---

## 常用命令速查

```bash
# 查看上游新增了什么
git log --oneline upstream/main ^HEAD

# 查看与上游的差异（文件级别）
git diff --name-only HEAD upstream/main

# 查看某个文件与上游的差异
git diff upstream/main -- frontend/src/components/MiniPetMascot.tsx

# 仅同步某个文件到上游版本（慎用，会丢失你的改动）
git checkout upstream/main -- <文件路径>
```
