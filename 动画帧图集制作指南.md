# 动画帧图集制作指南

> 目标：把静态 yae-fox.png 替换为有动画的精灵图集，让狐狸活起来。

---

## 整体流程

```
yae-fox.png（参考图）
    ↓ Kling AI 图生视频（5个状态）
各状态 MP4/WebM 视频
    ↓ FFmpeg 提帧
各状态 PNG 帧序列
    ↓ Python 脚本拼合
spritesheet.webp（替换现有占位）
```

---

## 第一步：用 Kling AI 生成各状态视频

以 `yae-fox.png` 为**参考图（垫图）**，保持角色一致性。

> ⚠️ **关键**：所有状态视频必须用同一张参考图作为 seed，角色才不会漂移。第一次多生成几个版本，选定满意的后固定这张图不换。

### 5个状态的描述提示词

**idle（闲置）**
```
The pink fox character stands in place with subtle breathing motion,
tail slowly swaying left and right, occasionally blinking.
Seamless 3-second loop. White background.
```

**working / running（工作中）**
```
The pink fox character leans slightly forward, ears perked up,
paws on desk, focused expression. Subtle body sway.
Seamless 2-second loop. White background.
```

**waiting（等待中）**
```
The pink fox character paces back and forth or spins in place,
looking impatient and restless.
Seamless 2-second loop. White background.
```

**waving / complete（完成庆祝）**
```
The pink fox character jumps up and waves a paw in celebration,
happy excited expression.
1.5-second animation, does NOT need to loop. White background.
```

**failed / error（报错沮丧）**
```
The pink fox character's ears droop down, tail hangs low,
head slowly shakes side to side with a sad expression.
Seamless 3-second loop. White background.
```

### 导出设置

- 背景：**纯白色**（便于后续抠图）
- 分辨率：尽量高（1080p 以上），缩小比放大质量好
- 格式：MP4 即可

---

## 第二步：FFmpeg 提帧

每个状态视频提取 PNG 帧序列。提帧数量对应图集的帧数（见下方规格）。

```bash
# 安装 FFmpeg（如未安装）
# Windows: https://ffmpeg.org/download.html 或 winget install ffmpeg

# idle：提取6帧，白底抠图
ffmpeg -i idle.mp4 -vf "fps=2,scale=192:208,chromakey=white:0.15:0.1" -pix_fmt rgba -frames:v 6 frames/idle_%03d.png

# working/running：提取6帧
ffmpeg -i working.mp4 -vf "fps=3,scale=192:208,chromakey=white:0.15:0.1" -pix_fmt rgba -frames:v 6 frames/running_%03d.png

# waiting：提取6帧
ffmpeg -i waiting.mp4 -vf "fps=3,scale=192:208,chromakey=white:0.15:0.1" -pix_fmt rgba -frames:v 6 frames/waiting_%03d.png

# waving（完成）：提取4帧
ffmpeg -i waving.mp4 -vf "fps=2.7,scale=192:208,chromakey=white:0.15:0.1" -pix_fmt rgba -frames:v 4 frames/waving_%03d.png

# failed（报错）：提取8帧
ffmpeg -i failed.mp4 -vf "fps=2.7,scale=192:208,chromakey=white:0.15:0.1" -pix_fmt rgba -frames:v 8 frames/failed_%03d.png
```

> ⚠️ `chromakey=white:0.15:0.1` 是白色背景抠图参数，如果边缘有白边残留，调大第二个数字（相似度）；如果角色本身被抠掉，调小它。

### 验证提帧效果

用 Windows 图片查看器或浏览器打开一张 PNG，确认：
- 背景是透明棋盘格（不是白色）
- 角色边缘干净，没有白边光晕
- 角色没有被过度抠图（毛发不残缺）

---

## 第三步：Python 拼合图集

确认 frames/ 目录下有各状态的帧序列后，运行拼合脚本。

新建文件 `tools/make_spritesheet.py`：

```python
from PIL import Image
import glob

CELL_W, CELL_H = 192, 208
COLS = 8

# 图集行顺序固定，不可更改（与代码中 ANIMATION_ROWS 对应）
# 没有素材的状态用 idle 第一帧填充占位
STATES = [
    ('idle',    'frames/idle_*.png',    6),
    ('run-right','frames/idle_*.png',   8),   # 暂时用 idle 占位，后续可单独制作
    ('run-left', 'frames/idle_*.png',   8),   # 同上
    ('waving',   'frames/waving_*.png', 4),
    ('jumping',  'frames/idle_*.png',   5),   # 暂时用 idle 占位
    ('failed',   'frames/failed_*.png', 8),
    ('waiting',  'frames/waiting_*.png',6),
    ('running',  'frames/running_*.png',6),
    ('review',   'frames/idle_*.png',   6),   # 暂时用 idle 占位
]

ROWS = len(STATES)
sheet = Image.new("RGBA", (CELL_W * COLS, CELL_H * ROWS), (0, 0, 0, 0))

for row_idx, (name, pattern, count) in enumerate(STATES):
    files = sorted(glob.glob(pattern))
    if not files:
        print(f"⚠️  {name}: 没找到帧文件 ({pattern})，跳过")
        continue
    for col_idx in range(count):
        src_path = files[col_idx % len(files)]  # 帧不够时循环
        img = Image.open(src_path).resize((CELL_W, CELL_H), Image.LANCZOS)
        sheet.paste(img, (col_idx * CELL_W, row_idx * CELL_H))
    print(f"✅ 行{row_idx} {name}: {min(len(files), count)}/{count} 帧")

out = "frontend/public/assets/builtin/fox-pink/spritesheet.webp"
sheet.save(out, "WebP", lossless=True)
print(f"\n完成：{out}")
```

运行：
```bash
# 在项目根目录 d:\Programs\DeskSprite 下运行
python tools/make_spritesheet.py
```

生成后 Vite 热更新会自动刷新，立刻能看到动画效果。

---

## 图集规格速查

| 行 | 状态 | 帧数 | FPS | 用途 |
|----|------|------|-----|------|
| 0 | idle | 6 | 2 | 闲置呼吸 |
| 1 | run-right | 8 | 8 | 向右拖动 |
| 2 | run-left | 8 | 8 | 向左拖动 |
| 3 | waving | 4 | 12 | **完成庆祝** |
| 4 | jumping | 5 | 6 | 鼠标悬停跳 |
| 5 | failed | 8 | 12 | **报错沮丧** |
| 6 | waiting | 6 | 6 | **等待中** |
| 7 | running | 6 | 6 | **工作中** |
| 8 | review | 6 | 12 | 审查中 |

**粗体**为 OpenClaw 状态联动会用到的关键状态，优先制作。

---

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 边缘有白边光晕 | chromakey 相似度太低 | 调大第二个参数，如 `0.2` |
| 角色毛发被抠掉 | chromakey 相似度太高 | 调小第二个参数，如 `0.1` |
| 帧与帧之间角色位置跳动 | 视频生成时角色位移 | 在 Kling 重新生成，选更稳定的版本 |
| 图集颜色偏暗 | WebP 有损压缩 | 确认 `lossless=True` 参数存在 |
| 动画播放太快/太慢 | FPS 与代码不匹配 | 不需要改代码，FPS 在 `codexPet.ts` 的 `STATE_FPS` 里定义 |

---

## 完成后

替换 `spritesheet.webp` 后，直接进入下一阶段：**OpenClaw 状态联动**——让狐狸根据 Agent 实际工作状态自动切换动画。
